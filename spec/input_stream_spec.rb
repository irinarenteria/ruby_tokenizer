require 'spec_helper'
require 'input_stream_module/input_stream'

describe InputStream do
  
  describe ".analyze" do
    context "when the input is empty" do
      pending
    end
    context "when the input is a file path that can't be found" do
      pending
    end

    context "when the input is a file that can be read" do
      pending
    end
  end
end